name: Build Lyre kernel

on:
  pull_request:
    paths-ignore:
      - "**.md"
  push:
    branches:
      - main
    paths-ignore:
      - "**.md"

jobs:
  lyre-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install build-essential -y

      - name: Attempt to build the Lyre kernel (debug)
        run: cd kernel && ./bootstrap && ./configure && make PROD=false && make maintainer-clean

      - name: Attempt to build the Lyre kernel (prod)
        run: cd kernel && ./bootstrap && ./configure && make PROD=true && make maintainer-clean
